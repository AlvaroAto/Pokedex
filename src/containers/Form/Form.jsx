import React from "react";

//styled-components
import FormContainer from "./Form-style";


const Form = ({ children }) => {
    <FormContainer>
        {children}
    </FormContainer>
};

export default Form;